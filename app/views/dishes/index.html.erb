<h1>All Dishes</h1>

    <ul class="ul-dishes-index">
        <% @dishes.each do |dish|%>
           <li><%= link_to image_tag(dish.image_URL, :class => 'thumb'), dish %><%= link_to dish.name, dish %></li>
        <% end %>
    </ul>
